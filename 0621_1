데이터프레임(DataFrame)은 판다스(Pandas) 라이브러리에서 지원하는 2차원 데이터 구조입니다. 데이터프레임은 행(row)과 열(column)로 구성되는데, 각각의 구성요소는 다음과 같습니다.
행(row): 각 행은 고유한 인덱스(index)를 가집니다. 인덱스는 보통 정수 또는 문자열로 구성되며, 각 행은 반드시 고유한 인덱스를 가져야 합니다.
열(column): 각 열은 고유한 이름(name)을 가집니다. 보통 문자열로 구성되며, 각 열은 반드시 고유한 이름을 가져야 합니다.
값(value): 데이터프레임 내부에 실제로 저장된 데이터입니다. 값은 모두 동일한 데이터 타입을 가지며, 보통 수치형 값이나 문자열로 구성됩니다.
인덱스(index): 행의 고유한 이름으로, 데이터프레임에서 인덱스는 행의 첫번째 열에 위치합니다. 행 인덱스는 변경이 가능하며, 중복되는 값을 가질 수 있습니다.
컬럼(column): 열의 이름으로, 데이터프레임에서 컬럼은 열의 첫번째 행에 위치합니다. 열 이름도 변경이 가능하지만, 열 이름이 중복되지 않도록 주의해야 합니다.
데이터(data): 데이터프레임에 실제로 저장된 값으로, 행과 열을 기준으로 이차원 배열 형식으로 저장됩니다. 데이터는 각각의 셀(cell)에 저장되며, 행과 열 이름을 이용해서 특정 위치의 셀에 접근할 수 있습니다.
